// A very simple protocol definition, consisting of only
// one message.
syntax = "proto3";

message command{
  string command = 1;
}

message Controller{
    int32 x = 1;
    int32 y = 2;
    bool sw = 3;
}
